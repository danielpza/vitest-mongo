# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - run: mise exec -- node --run fmt -- {staged_files}
      glob: "*"
      stage_fixed: true

    - run: mise exec -- node --run test -- --project=mongodb-memory-server --reporter=minimal
      root: tests
